pipeline {
    agent {
        docker 'docker.internal.kevinlin.info/infra/ci-base:0.0.6'
    }

    stages {
        stage('Install') {
            steps {
                sh 'npm-s3 install'
            }
        }
        stage('Test') {
            parallel {
                stage('Build') {
                    environment {
                        NODE_ENV = 'production'
                        REACT_ELEMENTAL_VERSION = sh(
                            script: 'cat package.json | jq -r \'.dependencies[\"react-elemental\"]\' | cut -c2-',
                            returnStdout: true
                        ).trim()
                    }
                    steps {
                        sh 'npm run build'
                    }
                }
                stage('Lint') {
                    steps {
                        sh 'npm run lint'
                    }
                }
                stage('Unit') {
                    steps {
                        sh 'npm run test'
                    }
                }
            }
        }
    }

    post {
        always {
            archive 'dist/index.html'
            deleteDir()
        }
    }
}
